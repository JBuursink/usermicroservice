spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:postgresql://database.csszytxwcf05.us-east-1.rds.amazonaws.com:5432/users
    driverClassName: org.postgresql.Driver
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080
